# Notes

```bash
# Create folder
npm create vite@latest vite-react-monaco -- --template react-ts

# In folder
npm install
npm install --save-dev tailwindcss postcss autoprefixer
npx tailwindcss init -p
```
